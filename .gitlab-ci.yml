pages:
  stage: deploy
  environment: staging
  script:
  - mkdir .public
  - cp -r * .public
  - mv .public public
  artifacts:
    paths:
    - public
  only:
  - master
deploy to production:
  environment: production
  only:
  - master
